# Core
numpy>=1.24.0
scipy>=1.11.0
opencv-python>=4.8.0
scikit-learn>=1.3.0
scikit-image>=0.22.0
pandas>=2.0.0
matplotlib>=3.7.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
PyYAML>=6.0
tqdm>=4.65.0
loguru>=0.7.0

# Deep learning & detection
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0

# 3D & storage
open3d>=0.17.0

# Optional advanced backends (install separately if needed):
# video-depth-anything  — see README
# pycolmap              — pip install pycolmap
# vggt                  — see official repo
