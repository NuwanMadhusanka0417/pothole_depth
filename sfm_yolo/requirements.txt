# --- Core scientific stack ---
numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
scikit-image>=0.21
scikit-learn>=1.3
tqdm>=4.65
PyYAML>=6.0

# --- Computer vision ---
opencv-python>=4.8
opencv-contrib-python>=4.8

# --- Deep learning / detection ---
torch>=2.0
torchvision>=0.15
ultralytics>=8.1            # YOLOv8

# --- Structure from Motion ---
# pycolmap is optional; the SfM runner falls back to the COLMAP CLI via
# subprocess if pycolmap is not installed. Install COLMAP separately
# (https://colmap.github.io/install.html).
pycolmap>=0.6 ; platform_system != "Windows"

# --- 3D / point clouds (optional, for visualization) ---
open3d>=0.17 ; python_version < "3.13"

# --- Notebooks / dev ---
jupyter>=1.0
ipykernel>=6.25
pytest>=7.4
